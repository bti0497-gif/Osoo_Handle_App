# 2026-09-06 현장 진단로그 검토

## 범위와 판정 기준

- 입력: `.tmp-diagnostics-current`의 JSONL 175개, 원본 1,233행.
- machine/runtime/id/created_at/area/action 조합으로 중복 117행을 제외한 1,116건 분석. JSON 파싱 오류 0건, PC 10대.
- 이벤트 시각 범위: 2026-09-02 19:20:27 ~ 2026-09-06 09:42:01 KST. 파일 다운로드 시각이 아닌 created_at 기준.
- 동명 40/41/42가 함께 포함되고 다른 PC는 39. 동명 42 기록은 175건이며 마지막 기록은 9월 5일 17:59경이다. 9월 6일까지 동명 상태를 확인한 것은 아니다.
- 자동 분석기는 현재 중복을 제거하지 않고 최상위 site_name으로만 분류하므로 직접 중복 제거 및 details의 현장 식별자를 대조했다. 동명 부산 이벤트도 춘천 이름의 업로드 파일에 포함된다.

## 1. 43 우선 후보: 공사입력도우미 사진 전달

9월 5일, 9월 4일자 분석사진을 사용하는 동명 양방향에서 재현됐다.

| 시각(KST) | 방향 | 결과 |
|---|---|---|
| 15:07 | 춘천 | 알칼리도·암모니아성질소 행 추가 성공, 질산성질소·인산염인 실패 |
| 15:08 | 춘천 | 앞 두 사진 유지, 뒤 두 사진 행 추가 성공 |
| 15:25 | 부산 | 앞 두 사진 성공, 뒤 두 사진 실패 |
| 15:26 | 부산 | 앞 두 사진 유지, 뒤 두 사진 재실패 |

확인된 사실:

- 로컬 준비 기록 5건 모두 readyPhotoCount=4, downloadFailureCount=0, localSaveFailureCount=0, selectedProjectIndex=0. 첫 분석 회차 준비 정책이 실행됐다.
- 해당 자동채우기에서 localAvailableCount=4, manifestFound=true, preparationStatus=ready.
- 실패 6건 모두 PHOTO_CHOOSER_NOT_OPENED. 각 3회 재시도에서 not-opened 및 matching-input-not-found/discoveredInputCount=0.
- 실패 지점은 로컬 사진 탐색 이후 웹페이지 파일 전달 단계다. 이번 실패를 파일명 불일치나 사진 미수집으로 설명할 수 없다.
- photo-row-added는 웹페이지 첨부 행 추가 확인이다. 도로공사 저장 버튼 이후 영구 저장 성공이나 Drive 업로드 성공까지 입증하지 않는다.

코드 검토:

- `electron/roadworkDumpHelper.cjs`의 attachRoadworkPhotoFile은 Runtime.evaluate에서 버튼을 찾아 scrollIntoView/focus/click하고 파일선택 이벤트를 기다린다. 버튼 발견과 click 호출 성공만 확인하며 실제 사용자 동작 인정 여부, 버튼 가시성·비활성 상태, 업로더 준비 상태는 기록하지 않는다.
- 모든 재시도에서 파일 input이 없으면 기존 직접 주입 fallback도 동작할 수 없다.
- `tools/diagnostic-runner/scenarios/roadwork-photo-injection.scenario.cjs`는 각 사진마다 새 가짜 debugger를 만들고 3·4번에는 파일 input이 있다고 가정한다. 실제 공유 웹뷰의 연속 동작과 input 0개 조건은 검증하지 못한다.

43 후보: 실제 공유 웹뷰에서 4종 연속 첨부 및 재시도 조건 재현, 클릭 실행 문맥·업로더 준비 상태 확인, 성공한 사진을 유지하면서 실패 항목만 복구. 사용자 활성화/스크롤 렌더링/업로더 상태 중 어느 것이 원인인지는 아직 미확정이며 로그만으로 하나를 단정하지 않는다. 회귀검증에 input 0개 및 앞 두 장 성공 후 뒤 두 장 실패 조건을 추가한다.

주요 원문:

- `.tmp-diagnostics-current/2026-09-05T06-07-26-365Z_동명휴게소(춘천방향)_diagnostics.jsonl` 42~45행
- `.tmp-diagnostics-current/2026-09-05T06-25-37-384Z_동명휴게소(춘천방향)_diagnostics.jsonl` 1~6행, 28~31행
- `.tmp-diagnostics-current/2026-09-05T06-26-37-797Z_동명휴게소(춘천방향)_diagnostics.jsonl` 1~2행

## 2. 동명 42: 서버 유지 및 일지 출력

- 서버 PID 768, Electron PID 12604가 6시간 간격 측정에서 24시간 유지됐다. 이번 관측 범위에서 반복 재시작 증거는 없다.
- HWP 출력 요청 4건은 HTTP 200, 각각 2,388 / 2,105 / 3,556 / 2,036ms. 예열을 포함한 현 버전 흐름에서 출력 응답이 빠른 사례다. 다른 PC의 39와 직접 성능비를 계산하거나 예열만의 효과로 단정하지 않는다.
- 서버 이벤트 루프 지연 4,048 / 3,343 / 2,333ms가 남았다. 실행 중 ping-timeout 복구 기록의 degradedElapsedMs는 17,879 / 1,796ms, 같은 서버 PID로 정상화됐다. 이 값은 감시기의 이상 감지부터 정상 확인까지의 시간이며 연속 완전 중단 시간을 뜻하지 않는다.
- 첫 번째 및 마지막 이벤트 루프 지연은 예열 요청 직후, 중간 지연은 게시판 조회 중 기록됐다. 시간적 연관성만 확인됐으며 원인 확정은 아니다.
- 43 진단 후보: 예열 요청 접수/실제 엔진 준비 완료/재사용/실패를 구분하고 단계별 시간 및 worker PID를 기록. 기존 예열 시점 정책은 유지.
- 서버 RSS는 6시간 이후 약 167~184MiB 범위로 측정됐다. EXCEL.EXE 전체 목록/소유 PID 기록이 없으므로 현장 Excel 누수 해소까지 판정할 수 없다.

## 3. 로그인·현장 분리·재고 표시 판정 한계

- 이번 자료에서 로그인 거절 기록은 없다. 동명 42 credential-scope-checked 5건 모두 identityMismatch=false, 기록된 windowSiteId와 serverResolvedSiteId 불일치 0건.
- 다른 외부 현장의 데이터가 화면에 나타났다는 기록은 이번 동명 42 자료에 없다. 내부 ID 일치만으로 외부 화면 전체의 현장 일치를 완전 보장할 수는 없다.
- 대시보드 표시 품목, 재고 수치, 구매량 분모를 담은 정상 결과 로그는 없어 208 표시나 분모 정확성을 이번 자료만으로 확인할 수 없다. 앞서 정한 43 별칭 추정 제거 후보는 유지한다.

## 4. 39 현장 관찰 사항

- 횡성 강릉 HWP 응답 68.321초 및 62.063초, 모두 HTTP 200. 서버 PID 5136/uptime이 이어져 출력 지연을 서버 재시작으로 볼 근거는 없다.
- 홍천 양양 session-keepalive-failed 14건은 로그인 경로 응답이 반복된 기록. 밀양 울산 unexpected-login-page 2건. 특히 로그인 화면 이동과 keepalive 정상 판정이 어긋나는 사례가 있어 실제 업무 인증 상태 판정 보강 후보로 남긴다. 외부 세션 만료 원인은 미확정.
- 밀양 울산 시스템 메모리 부족 경고 43건, 최소 여유 4.3%. 이 값은 시스템 전체 메모리이므로 Excel이나 앱 누수로 단정하지 않는다.
- 전체 error 26건 중 25건은 지연 임계값 초과지만 HTTP 200으로 완료된 요청, 나머지 1건은 동명 41 이벤트 루프 지연 17.179초다. 42 error=0이어도 사진 실패는 warn으로 기록되므로 정상 판정 근거로 삼지 않는다.

## 다음 버전 권고

1. 사진 3·4번째 전달 실패 재현·보완 및 해당 회귀 시나리오를 우선 추가.
2. 기존 43 후보인 재고 위젯 별칭 추정 제거 유지.
3. 엔진 준비 완료·소유 프로세스 및 순간 지연 원인 진단 보강 검토.
4. 39의 외부 로그인 상태 판정 불일치는 현재 코드와 재현을 대조해 후속 판단.

이번 작업은 로그 및 코드 분석과 이 보고서 작성이다. 앱 구현 수정·빌드·배포는 수행하지 않았다.
