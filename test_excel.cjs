const ExcelJS = require('exceljs'); const path = require('path'); async function run() { const wb = new ExcelJS.Workbook(); await wb.xlsx.readFile(path.join(process.env.APPDATA, 'Osoo_Handle_App', 'documents', 'daily_work_log_template.xlsx')); const model = wb.definedNames && Array.isArray(wb.definedNames.model) ? wb.definedNames.model : []; console.log(model.map(i => i.name)); } run();
